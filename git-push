# 1. Git repositoryi başlat (eğer yapmadıysanız)
git init

# 2. Tüm dosyaları staging'e ekle
git add .

# 3. İlk commit'i yap
git commit -m "Initial commit"

# 4. Branch'i 'main' olarak ayarla
git branch -M main

# 5. Remote repository'i ekle (eğer yapmadıysanız)
git remote add origin https://github.com/onder7/File-Folder-Sync.git

# 6. Şimdi push işlemini dene
git push -u origin main

GitHub'daki açıklamanıza (README.md gibi) resim eklemek için aşağıdaki adımları takip edebilirsiniz:
![Resim Açıklaması](images/resim_adı.png)


